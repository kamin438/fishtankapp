import breakpoints from './token-groups/breakpoints.json.esnext';
import darkColorScheme from './token-groups/color.dark.json.esnext';
import depth from './token-groups/depth.json.esnext';
import legacyTokens from './token-groups/legacy-tokens.json.esnext';
import lightColorScheme from './token-groups/color.light.json.esnext';
import motion from './token-groups/motion.json.esnext';
import shape from './token-groups/shape.json.esnext';
import spacing from './token-groups/spacing.json.esnext';
import typography from './token-groups/typography.json.esnext';
import zIndex from './token-groups/z-index.json.esnext';
import { tokensToRems } from './utilities.esnext';

/**
 * Values to convert to CSS custom properties.
 * @example {background: '#000'} // --p-background: #000;
 */

const osColorSchemes = {
  light: 'light',
  dark: 'dark'
};
/**
 * Polaris color schemes and their associated color tokens.
 */

const colorSchemes = {
  light: lightColorScheme,
  dark: darkColorScheme
};
const tokens = {
  breakpoints: tokensToRems(breakpoints),
  colorSchemes,
  depth,
  legacyTokens: tokensToRems(legacyTokens),
  motion,
  shape: tokensToRems(shape),
  spacing: tokensToRems(spacing),
  typography: tokensToRems(typography),
  zIndex
};

export { osColorSchemes, tokens };
