import { Tokens, ColorScheme, TokenGroup, OSColorSchemes } from '../../tokens';
/**
 * Creates CSS Rules for each color-scheme.
 * @example:
 * [p-color-scheme="light"] {...}
 * [p-color-scheme="dark"] {...}
 * [p-color-scheme="dim"] {...}
 */
export declare function getColorSchemeRules(tokens: Tokens, osColorSchemes: OSColorSchemes): string;
/**
 * Creates static CSS custom properties.
 * Note: These values don't vary by color-scheme.
 */
export declare function getStaticCustomProperties(tokens: Tokens): string;
/**
 * Creates CSS declarations for a given color-scheme.
 */
export declare function getColorSchemeDeclarations(colorScheme: ColorScheme, tokens: Tokens, osColorSchemes: OSColorSchemes): string;
/**
 * Creates CSS custom properties for a given tokens object.
 */
export declare function getCustomProperties(tokens: TokenGroup): string;
/**
 * Concatenates the `keyframes` token-group into a single string.
 */
export declare function getKeyframes(motion: TokenGroup): string;
/**
 * Adapted from: https://github.com/argyleink/gui-challenges/blob/main/color-schemes/style.css
 */
export declare const styles: string;
//# sourceMappingURL=styles.d.ts.map